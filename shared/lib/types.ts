export type pagesProps = {
    title: string,
    selectionData?: Array<any>;
    descriptionData?: object;
}

export type FormData = {
    name: string;
    email: string;
    text: string;
  };
  